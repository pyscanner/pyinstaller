"""
This is a sample Python main file that gets converted to an executable.
"""
from PyInstaller import __version__ as pyinstaller_version

if __name__ == '__main__':
    print(f'Hello PyInstaller - v__name{pyinstaller_version}')
